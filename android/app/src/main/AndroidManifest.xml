<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- v0.3.4: needed to drive the Vibrator service directly for the in-game
         Haptics toggle. Normal install-time permission. -->
    <uses-permission android:name="android.permission.VIBRATE"/>
    <!-- Telemetry: opt-in Firebase Analytics only (docs/telemetry-events.md). -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <!-- Never collect the advertising ID (no ads, no ad personalization).
         The Play measurement SDK also merges in the Privacy Sandbox
         AdServices permissions; this game does no ads, attribution or
         conversion measurement, so all three are force-removed. Verify with
         `aapt2 dump badging` after touching this block. -->
    <uses-permission android:name="com.google.android.gms.permission.AD_ID"
        tools:node="remove"/>
    <uses-permission android:name="android.permission.ACCESS_ADSERVICES_AD_ID"
        tools:node="remove"/>
    <uses-permission android:name="android.permission.ACCESS_ADSERVICES_ATTRIBUTION"
        tools:node="remove"/>
    <application
        android:label="Pyregrove"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- THE STEADY RENDERER (2026-09-01): opt out of Impeller on Android,
             keep Skia. Our install base skews Adreno 5xx / low-end Mali:
             Flutter's engine denylists Vulkan on Adreno <=650, dropping those
             devices to Impeller GLES (~28fps vs Skia ~54 on Adreno 506,
             flutter/flutter#187009), and Android-10 Mali carries an
             ImageDecoder SIGABRT under Impeller (flutter/flutter#190640 —
             this game decodes sprite PNGs). Pure-2D CustomPaint rendering
             gains nothing from Impeller here. Mirrors the same change in
             emberdelve. Re-evaluate on every Flutter upgrade. -->
        <meta-data
            android:name="io.flutter.embedding.android.EnableImpeller"
            android:value="false" />
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
        <!-- Analytics collection is OFF until the player opts in at runtime
             (TelemetryService.setAnalyticsConsent). Prominent-disclosure
             consent model; see docs/telemetry-events.md. -->
        <meta-data
            android:name="firebase_analytics_collection_enabled"
            android:value="false" />
        <meta-data
            android:name="google_analytics_default_allow_ad_personalization_signals"
            android:value="false" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- url_launcher (directive 2026-09-05b): let the "More from Tsoro
             Studios" row resolve the Play app and a browser on API 30+. -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="market"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="https"/>
        </intent>
    </queries>
</manifest>
