// version.dart — single source of the user-visible build label.
//
// Shown in the title-screen corner so alpha testers can report exactly
// which build they're on (DEMAND pillar 4: honest presentation). Kept in
// lockstep with pubspec.yaml by test/version_test.dart — bump both or the
// suite fails.
const String kAppVersion = '1.0.0-alpha.24+36';
